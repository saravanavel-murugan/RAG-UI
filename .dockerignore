# Python
__pycache__/
*.pyc
*.pyo
venv/
.venv/
env/

# Secrets - never bake into the image
.env

# NOTE: hr_faiss_index/ is intentionally NOT ignored, so the prebuilt
# FAISS index is copied into the image (Streamlit loads it at startup).

# OS / editor junk
.DS_Store
Thumbs.db

# Git
.git/
.gitignore
