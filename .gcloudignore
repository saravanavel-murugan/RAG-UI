# Files excluded from `gcloud` uploads.
.gcloudignore
.git/
.gitignore

# Python
__pycache__/
*.pyc
venv/
.venv/
env/

# Secrets
.env

# NOTE: hr_faiss_index/ is intentionally NOT ignored, so the prebuilt
# FAISS index is uploaded and copied into the image.

# OS
.DS_Store
Thumbs.db
